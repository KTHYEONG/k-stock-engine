"""One-off recovery of quarantined ``legacy_document`` filings (temporary tool).

Lifecycle: this package is deleted after a successful run. What stays
permanently is the trust contract (``docs/specs/legacy_verified_kind_spec.md``)
and the benchmark report under ``docs/research/``. See
``docs/specs/legacy_recovery_tool_spec.md``.
"""
